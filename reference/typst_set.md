# Create a Typst set rule

Used to create all Typst rules in `r2typ`. Arguments are parsed and then
translated to Typst.

## Usage

``` r
typst_set(name, ...)
```

## Arguments

- name:

  Function name

- ...:

  Any kind of named or not arguments.

## Value

A character vector with well formatted Typst.
